__pycache__/
*.pyc

# Kokoro weights — ~340MB, fetched by the curl commands tts.py prints if missing
models/

# Your enrolled voiceprint, and the gate's audit log
voiceprint.npy
decisions.jsonl

node_modules/

# The brain, bundled. Source is app/renderer/brain/; `npm run build` rebuilds it
# and `npm start` runs that first, so this is output and never edited by hand.
app/renderer/js/brain.js
app/renderer/js/brain.js.map

# Sweep output — tens of MB of WAV, and `python3 sweep.py` regenerates all of it.
# The eight samples/*.wav stay tracked as the set to listen to at a glance.
samples/sweep-*/
